{
	"name": "keyzori-workspace",
	"version": "0.4.1",
	"license": "Apache-2.0",
	"description": "Self-hosted software license management server with an operator CLI and client SDK.",
	"author": "Keyzori contributors",
	"keywords": [
		"keyzori",
		"licensing",
		"software-licensing",
		"license-manager",
		"license-server",
		"license-key",
		"subscriptions",
		"usage-based-licensing",
		"device-limits",
		"concurrent-sessions",
		"self-hosted",
		"open-source",
		"foss",
		"typescript",
		"bun",
		"elysia",
		"postgresql",
		"redis",
		"drizzle-orm",
		"sdk",
		"cli"
	],
	"packageManager": "bun@1.4.0",
	"type": "module",
	"private": true,
	"scripts": {
		"setup": "bun install --frozen-lockfile && bun run db:migrate",
		"dev": "turbo run dev --filter=keyzori-server",
		"dev:server": "turbo run dev --filter=keyzori-server",
		"dev:server:binary": "bun run build:server && bun run server",
		"start": "turbo run start --filter=keyzori-server",
		"server": "turbo run start --filter=keyzori-server",
		"cli": "bun run --cwd apps/server cli",
		"cli:help": "bun run --cwd apps/server cli --help",
		"cli:binary": "bun run --cwd apps/server cli:binary",
		"build": "turbo run build",
		"build:server": "turbo run build --filter=keyzori-server",
		"build:sdk": "turbo run build --filter=keyzori",
		"smoke:packages": "bun -e \"import { LicenseClient } from './apps/sdk/dist/index.js'; if (typeof LicenseClient !== 'function') throw new Error('SDK export missing')\" && bun run cli:binary --help",
		"test": "bun test apps tests",
		"test:coverage:core": "bun test --config bunfig.coverage.toml --coverage apps/server/src/test/AdminService.test.ts apps/server/src/test/LicenseService.test.ts apps/server/src/test/ActivityService.test.ts && bun test --config bunfig.coverage.toml --coverage apps/sdk/src/test/sdk.test.ts",
		"test:server": "bun run --cwd apps/server test",
		"test:cli": "bun test apps/server/src/test/cli.test.ts",
		"test:sdk": "bun run --cwd apps/sdk test",
		"test:sdk:compiled": "bun run build:sdk && KEYZORI_SDK_COMPILED_TEST_ENABLED=true bun test tests/sdkCompiledBinary.test.ts",
		"test:flow": "bun test tests/productFlow.test.ts",
		"test:live": "bun run build:server && bun run --cwd apps/server ../../tests/liveFlow.ts",
		"typecheck": "turbo run typecheck && bunx tsc --project tests/tsconfig.json",
		"check": "bun run typecheck && bun run test && bun run test:coverage:core && bun run release:verify && bun run lint",
		"check:server": "bun run --cwd apps/server typecheck && bun run test:server",
		"check:cli": "bun run --cwd apps/server typecheck && bun run test:cli",
		"check:sdk": "bun run --cwd apps/sdk typecheck && bun run test:sdk",
		"db:generate": "bun run --cwd apps/server db:generate",
		"db:check": "bun run --cwd apps/server db:check",
		"db:migrate": "bun run --cwd apps/server db:migrate",
		"db:push": "bun run --cwd apps/server db:push",
		"db:studio": "bun run --cwd apps/server db:studio",
		"docker:build": "docker build --file apps/server/Dockerfile --tag keyzori-license-server .",
		"docker:build:server": "bun run docker:build",
		"publish:packages": "bun run --cwd apps/sdk publish:package",
		"publish:sdk": "bun run --cwd apps/sdk publish:package",
		"release:verify": "bun run scripts/verifyRelease.ts",
		"lint": "bunx @biomejs/biome check .",
		"format": "bunx @biomejs/biome format --write .",
		"audit": "bun audit --audit-level=high"
	},
	"workspaces": [
		"apps/server",
		"apps/sdk"
	],
	"devDependencies": {
		"@biomejs/biome": "2.5.10",
		"@types/bun": "1.4.0",
		"@types/node": "26.3.0",
		"turbo": "2.10.12",
		"typescript": "7.0.2"
	}
}
