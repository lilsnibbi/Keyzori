{
	"name": "@keyzori/dash",
	"version": "0.1.0",
	"description": "Standalone administrative dashboard for a Keyzori server.",
	"license": "Apache-2.0",
	"type": "module",
	"private": true,
	"scripts": {
		"start": "bun run src/index.ts",
		"dev": "bun run --watch src/index.ts",
		"test": "bun test",
		"typecheck": "bunx tsc --noEmit"
	}
}
